// Write your code in this file!
let currentUser = "Mercy Wanjiru";
let excited = "!";
let welcomeMessage = `Welcome to Flatbook, ${currentUser}${excited}`;
let excitedWelcomeMessage = welcomeMessage.toUpperCase();
let shortGreeting = `Welcome, ${currentUser.slice(0, 1)}${excited}`;
console.log(shortGreeting);
